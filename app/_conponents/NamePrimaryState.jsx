

const NamePrimaryState = ({ divClassName, className, text }) => {
  return (
    <div className={divClassName}>
        <button className={className}>{text}</button>
    </div>
  )
}

export default NamePrimaryState